# macOS
.DS_Store

# Xcode — user-specific state
xcuserdata/
*.xcuserstate
*.xcuserdatad/

# Xcode — build output
build/
DerivedData/
*.xcarchive
*.xcframework

# Swift Package Manager
.swiftpm/
.build/
# Package.resolved is intentionally committed so dependency versions are reproducible

# Bundled Core ML pipeline assets. Two storage tiers live under Resources/:
#
#   Heavy (NOT committed) — the four .mlpackage / .mlmodelc artifacts plus
#   any compiled bundle output. ~500 MB total; well past GitHub's 100 MB
#   single-file cap. As of Phase 8 these are runtime-downloaded by
#   BundledMLModelManager from huggingface.co/slaughters85j/pocket-tts-coreml
#   on first launch, so a fresh clone doesn't need them on disk to build.
#
#   Light (COMMITTED) — the 7 stock voice KV state safetensors, tokenizer.model,
#   and tokenizer_vocab.json. ~6 MB total, CC-BY 4.0, never changes per build.
#   Whitelisted below so `git status` picks them up.
mimika-ai-voice-studio/Resources/

# Whitelist the small bundled assets so a clone-and-build works without any
# external setup. Stock-only — custom voices ALWAYS live in the user's sandbox
# container via the in-app Voice Manager, never in source.
!mimika-ai-voice-studio/Resources/tokenizer.model
!mimika-ai-voice-studio/Resources/tokenizer_vocab.json
!mimika-ai-voice-studio/Resources/voice_kv_states/
!mimika-ai-voice-studio/Resources/voice_kv_states/alba.safetensors
!mimika-ai-voice-studio/Resources/voice_kv_states/azelma.safetensors
!mimika-ai-voice-studio/Resources/voice_kv_states/cosette.safetensors
!mimika-ai-voice-studio/Resources/voice_kv_states/fantine.safetensors
!mimika-ai-voice-studio/Resources/voice_kv_states/javert.safetensors
!mimika-ai-voice-studio/Resources/voice_kv_states/jean.safetensors
!mimika-ai-voice-studio/Resources/voice_kv_states/marius.safetensors

# Test output / scratch files
*.wav
*.aiff

# Test fixtures (committed deliberately — small + needed for parity tests)
!mimika-ai-voice-studioTests/Fixtures/*.wav

# Phase 10 LavaSR fixtures and per-stage Python reference dumps are
# entirely local. Regenerated by
#   scripts/generate_lavasr_phase10_fixtures.py  (creates *.wav)
#   scripts/validate_lavasr_enhancement.py --full (creates *.npy)
# Swift parity tests skip gracefully if the fixtures dir is empty;
# run the two scripts before `xcodebuild test` to enable them.
mimika-ai-voice-studioTests/Fixtures/lavasr_phase10/

# Profiles
*.trace
*.app.dSYM

# IDE
.idea/
.vscode/
*.swp
*.swo

# Understand Anything (knowledge graph analysis)
.understand-anything/

# Misc
*.log
.env
.env.local

/tools
.gstack/
